using System;

class Program
{
    static void Main()
    {
        string input = "programming";
        string result = "";

        foreach (char c in input)
        {
            if (!result.Contains(c))
            {
                result += c;
            }
        }

        Console.WriteLine("Result: " + result);
    }
}
