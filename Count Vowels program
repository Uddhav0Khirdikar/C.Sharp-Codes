using System;

class Program
{
    static void Main()
    {
        string text = "Hello World";
        int count = 0;

        foreach (char c in text.ToLower())
        {
            if ("aeiou".Contains(c))
                count++;
        }

        Console.WriteLine("Vowels: " + count);
    }
}
