using System;

class Program
{
    static void Main()
    {
        Random random = new Random();
        int[] arr = new int[10];

        for (int i = 0; i < arr.Length; i++)
        {
            arr[i] = random.Next(1, 100);
            Console.Write(arr[i] + " ");
        }
    }
}
