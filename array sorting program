using System;

class Program
{
    static void Main()
    {
        int[] numbers = { 5, 2, 8, 1, 4 };

        Array.Sort(numbers);

        foreach (int n in numbers)
        {
            Console.WriteLine(n);
        }
    }
}
